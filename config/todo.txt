 
1). < + number
 
 auto stride = torch::ExpandingArray<2>({2, 2});
 torch::ExpandingArray<2> shape({-1, 256 * 6 * 6});

2). mix and switching between the chars on base and char layers: << "/" << 

std::count << "Train:" << epoch << " " << end << "/" << data_size

sol: 
chars_split_layer

3). char layer, base layer char, chair layer
char layer, base layer, char layer, base layer

<< "\t"

sol: 
chars_split_layer

4). char, base, number
You have to type the change layer keys two times

make -j8

